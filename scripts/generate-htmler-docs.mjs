import fs from 'node:fs';
import path from 'node:path';

const output = '/Users/williambarr/Documents/HTMLerFiles/DeskDashboard';
fs.mkdirSync(output, { recursive: true });

const css = `
:root{color-scheme:dark;--bg:#14171a;--card:#1d2126;--ink:#e8eaed;--muted:#a4acb5;--faint:#737c86;--border:#2b3138;--border-strong:#3a424b;--accent:#6cc394;--accent-soft:#1c3128;--warn:#e0a458;--warn-soft:#33280f;--shadow:0 1px 2px rgba(0,0,0,.3),0 6px 20px rgba(0,0,0,.35);--radius:14px}
*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--bg);color:var(--ink);font:16px/1.65 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}main{max-width:880px;margin-inline:auto;padding:52px clamp(16px,4vw,40px) 90px}h1{font-size:clamp(26px,5vw,38px);line-height:1.12;margin:0 0 12px}h2{font-size:clamp(21px,3.5vw,28px);line-height:1.2;margin:48px 0 16px}h3{font-size:clamp(17px,3vw,21px);line-height:1.25;margin:28px 0 10px}p{margin:0 0 14px;max-width:72ch}a{color:var(--accent);overflow-wrap:anywhere}code{font:0.9em ui-monospace,SFMono-Regular,Menlo,monospace;color:#bfe7d1;background:#121513;padding:2px 5px;border-radius:5px;overflow-wrap:anywhere}pre{overflow-x:auto;max-width:100%;padding:16px;background:#101214;border:1px solid var(--border);border-radius:10px}pre code{padding:0;background:none}.lede{font-size:18px;color:var(--muted);max-width:68ch}.crumbs{font-size:13px;color:var(--faint);margin-bottom:18px}.card{background:var(--card);border:1px solid var(--border);border-radius:var(--radius);padding:22px;margin:14px 0;box-shadow:var(--shadow)}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(280px,100%),1fr));gap:14px}.grid .card{margin:0}.tag{display:inline-block;font-size:11px;font-weight:750;letter-spacing:.08em;text-transform:uppercase;border:1px solid var(--border-strong);border-radius:999px;padding:3px 8px;color:var(--muted);margin-right:5px}.must{color:var(--accent);background:var(--accent-soft)}.important{color:var(--warn);background:var(--warn-soft)}.optional{color:var(--muted)}ul,ol{padding-left:22px}li{margin:6px 0}.tablewrap{overflow-x:auto;border:1px solid var(--border);border-radius:var(--radius);margin:14px 0}table{width:100%;border-collapse:collapse;min-width:620px;background:var(--card)}th,td{text-align:left;vertical-align:top;padding:12px 14px;border-bottom:1px solid var(--border)}th{color:var(--accent);font-size:12px;text-transform:uppercase;letter-spacing:.06em}.flow{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;color:#c7d0d9;background:#101214;border:1px solid var(--border);border-radius:var(--radius);padding:18px;white-space:pre-wrap;overflow-wrap:anywhere}.three{border-color:var(--accent);background:linear-gradient(135deg,var(--accent-soft),var(--card))}.warning{border-color:#68512b;background:var(--warn-soft)}.file{display:inline-block;overflow-wrap:anywhere;max-width:100%;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;color:#bfe7d1}.skipto{position:fixed;top:50%;left:18px;transform:translateY(-50%);z-index:60;display:flex;flex-direction:column;gap:5px;background:var(--card);border:1px solid var(--border);border-radius:14px;padding:12px 12px 13px;box-shadow:var(--shadow);width:172px}.skipto a{text-decoration:none;color:var(--muted);font-size:13px;padding:6px 10px;border-radius:8px;transition:.15s}.skipto a:hover{background:var(--bg);color:var(--accent)}.skipto-toggle{display:none;align-items:center;justify-content:center;width:40px;height:40px;padding:0;background:none;border:0;color:var(--muted);cursor:pointer;border-radius:9px}.skipto-toggle:hover{color:var(--accent);background:var(--bg)}.skipto-toggle svg{transition:.15s}.skipto-menu{display:flex;flex-direction:column;gap:5px}.navlabel{font-size:11px;text-transform:uppercase;letter-spacing:.1em;color:var(--faint);padding:3px 10px}.footerlinks{display:flex;flex-wrap:wrap;gap:8px;margin-top:44px}.footerlinks a{background:var(--card);border:1px solid var(--border);border-radius:9px;padding:8px 11px;text-decoration:none}
@media(max-width:1180px){.skipto{top:10px;left:10px;transform:none;width:auto;padding:5px;border-radius:12px}.skipto-toggle{display:flex}.skipto-menu{display:none;padding:4px 6px 8px;min-width:172px}.skipto:hover .skipto-menu,.skipto:focus-within .skipto-menu,.skipto.open .skipto-menu{display:flex}.skipto:hover .skipto-toggle svg,.skipto:focus-within .skipto-toggle svg,.skipto.open .skipto-toggle svg{transform:rotate(180deg)}main{padding-top:72px}[id]{scroll-margin-top:80px}}
@media(max-width:720px){.grid{grid-template-columns:1fr}.card{padding:15px}.tablewrap{border-radius:10px}main{padding-inline:16px}}
`;

const reviewCss = `
h2{padding-left:12px;border-left:3px solid var(--accent)}
.reviewmeta{display:flex;flex-wrap:wrap;gap:7px;margin:18px 0 28px}
.reviewmeta span{font-size:12px;color:var(--muted);border:1px solid var(--border);background:var(--card);border-radius:999px;padding:4px 9px}
.quickread{border-color:var(--border-strong)}
`;

const navScript = `<script>var nav=document.querySelector('.skipto');nav.querySelector('.skipto-toggle').addEventListener('click',function(){nav.classList.toggle('open')});document.addEventListener('click',function(e){if(!nav.contains(e.target))nav.classList.remove('open')});nav.querySelectorAll('.skipto-menu a').forEach(function(a){a.addEventListener('click',function(){nav.classList.remove('open');requestAnimationFrame(function(){document.activeElement&&document.activeElement.blur()})})});</script>`;
const links = [
  ['index.html','Codebase Hub'],['architecture.html','Architecture'],['framework.html','DashboardKit Core'],
  ['renderers.html','Renderers'],['application.html','App & Widgets'],['build-test-ops.html','Build, Test & Ops'],
  ['file-map.html','File Map']
];

function page({file,title,sentence,sections,remember,todo}){
  const nav = sections.map(s=>`<a href="#${s.id}">${s.nav ?? s.title}</a>`).join('');
  const body = sections.map(s=>`<section id="${s.id}"><h2>${s.title}</h2>${s.html}</section>`).join('\n');
  const footer = links.filter(([f])=>f!==file).map(([f,l])=>`<a href="${f}">${l}</a>`).join('');
  const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${title}</title><style>${css}${reviewCss}</style></head><body>
  <nav class="skipto" aria-label="Skip to section"><button class="skipto-toggle" aria-label="Skip to section menu"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg></button><div class="skipto-menu"><div class="navlabel">Skip to</div><a href="#one">One sentence</a>${nav}<a href="#remember">Remember 3</a><a href="#next">Next actions</a></div></nav>
  <main><div class="crumbs">DeskDashboard / HTMLer Codebase Guide</div><h1>${title}</h1><p id="one" class="lede"><strong>One-sentence version:</strong> ${sentence}</p><div class="reviewmeta"><span>Source-checked</span><span>ADHD scan pass</span><span>Responsive to 360px</span></div><div class="card quickread"><span class="tag must">Read first</span><p>Scan the section headings and priority cards. Then use the left navigation to jump only to the detail needed for your current task.</p></div>${body}
  <section id="remember"><h2>Remember Only 3 Things</h2><div class="card three"><ol>${remember.map(x=>`<li>${x}</li>`).join('')}</ol></div></section>
  <section id="next"><h2>What To Do Next</h2><div class="card"><ol>${todo.map(x=>`<li>${x}</li>`).join('')}</ol></div></section>
  <div class="footerlinks">${footer}</div></main>${navScript}</body></html>`;
  fs.writeFileSync(path.join(output,file),html);
}

page({file:'index.html',title:'DeskDashboard Codebase Hub',sentence:'DeskDashboard is a Swift Package that separates a reusable dashboard engine, reusable renderers, and one Raspberry Pi dashboard application.',sections:[
{id:'start',title:'What This Guide Covers',html:`<div class="grid"><div class="card"><span class="tag must">Must Know</span><h3>Reusable core</h3><p>DashboardKit owns dashboard state, widget lifecycle, semantic content, layout, themes, clocks, and snapshots.</p><p><a href="framework.html">Read the core guide</a></p></div><div class="card"><span class="tag important">Important</span><h3>Rendering adapters</h3><p>Console, web, and SwiftCrossUI backends consume snapshots without defining app-specific data.</p><p><a href="renderers.html">Read the renderer guide</a></p></div><div class="card"><span class="tag optional">Context</span><h3>The appliance</h3><p>The App layer composes six feature families into ten widget instances, plus push ingestion, board arrangements, and executable entry points.</p><p><a href="application.html">Read the application guide</a></p></div></div>`},
{id:'shape',title:'Repository Shape',html:`<div class="tablewrap"><table><thead><tr><th>Path</th><th>Responsibility</th><th>Reusable?</th></tr></thead><tbody><tr><td><span class="file">Sources/Framework/DashboardKit</span></td><td>Dashboard engine and semantic UI contracts</td><td>Yes; current library product</td></tr><tr><td><span class="file">Sources/Framework/DashboardHTTPServer</span></td><td>Small socket HTTP server</td><td>Potentially, but currently an internal target</td></tr><tr><td><span class="file">Sources/Renderers</span></td><td>Console, dev web, and SwiftCrossUI adapters</td><td>Designed to be reusable; not exposed as library products</td></tr><tr><td><span class="file">Sources/App</span></td><td>DeskDashboard widgets, services, ingest, composition, executables</td><td>No; example/integration application</td></tr><tr><td><span class="file">Tests</span></td><td>Framework and widget behavior tests</td><td>Verification only</td></tr><tr><td><span class="file">scripts / producers</span></td><td>Pi build, kiosk install, and macOS data producer</td><td>Operations tooling</td></tr></tbody></table></div>`},
{id:'flow',title:'End-to-End Mental Model',html:`<div class="flow">Service → WidgetModel → Widget.render()
        ↓
WidgetContent → WidgetLayout → WidgetView
        ↓
DashboardSnapshot + WidgetPlacement
        ↓
Console / Dev Web / SwiftCrossUI renderer
        ↓
User action name → DashboardRunner.perform → InteractiveWidget</div><p>The framework moves data toward renderers as immutable semantic snapshots. Input travels back as widget ID plus action name.</p>`},
{id:'priority',title:'Reading Priority',html:`<div class="card"><p><span class="tag must">Must Know</span> Read <code>Package.swift</code>, <code>Dashboard.swift</code>, <code>DashboardRunner.swift</code>, <code>Widget.swift</code>, <code>WidgetContent.swift</code>, and <code>Composition.swift</code>.</p><p><span class="tag important">Important</span> Read the renderer model, board column DSL, and one service-backed widget end to end.</p><p><span class="tag optional">Optional</span> Read GTK-specific support and Pi deployment scripts only when changing native UI or the appliance.</p></div>`}
],remember:['The Framework/Renderers/App separation is the organizing principle.','Widgets emit semantic content; renderers decide pixels and platform behavior.','The live Pi app is both a product and the integration proving ground for the library.'],todo:['Open the Architecture page for the system-wide data flow.','Use the File Map to locate the owner of a behavior before editing.','Use Build, Test & Ops before changing deployment or platform code.']});

page({file:'architecture.html',title:'Architecture and Data Flow',sentence:'The package uses a one-way snapshot pipeline with named actions returning to interactive widgets.',sections:[
{id:'layers',title:'Layer Boundaries',html:`<div class="grid"><div class="card"><h3>Framework</h3><p>Defines values and protocols. It does not know the DeskDashboard widget catalog or native view hierarchy.</p></div><div class="card"><h3>Renderers</h3><p>Interpret snapshots and semantic WidgetView trees. They may depend on platform UI libraries.</p></div><div class="card"><h3>Application</h3><p>Selects services, widgets, themes, layouts, ingest routes, arrangements, and process arguments.</p></div></div><div class="card warning"><span class="tag important">Boundary risk</span><p>The package manifest applies macOS 13 globally, and only DashboardKit is currently a library product. Those distribution constraints are stricter than the conceptual layer boundary.</p></div>`},
{id:'lifecycle',title:'Dashboard and Widget Lifecycle',html:`<div class="flow">Dashboard.add(widget)
  → resolve stable/preferred WidgetID
  → detach any replaced widget
  → build DashboardEnvironment
  → widget.attach(environment)
  → compute layout placements

clock tick
  → Dashboard.tick(date)
  → visibility + refresh-rate gate
  → widget.tick(tick, environment)
  → renderer reads attachedWidgetSnapshots

Dashboard.remove(widget)
  → widget.detach()
  → remove order entry
  → recompute placements</div><p>Dashboard is a value type, but attached widgets are type-erased internally. DashboardRunner wraps it in a mutable reference type for clocks and host wiring.</p>`},
{id:'render',title:'Render Pipeline',html:`<ol><li>A widget produces <code>WidgetContent</code>: title, primary/secondary/accessory text, progress, timing, play state, and metadata.</li><li>Its <code>WidgetLayout</code> maps content into a backend-neutral <code>WidgetView</code> tree.</li><li>The dashboard combines content, widget configuration, and <code>WidgetPlacement</code> into <code>AttachedWidgetSnapshot</code>.</li><li>A renderer maps semantic text roles, stacks, regions, progress, and interactions to its backend.</li></ol><p>This is the most important portability seam in the codebase.</p>`},
{id:'input',title:'Interaction Flow',html:`<div class="flow">Renderer detects tap/hold
 → emits (WidgetID, action String)
 → app calls DashboardRunner.perform
 → Dashboard performs on matching InteractiveWidget
 → widget mutates service/model state
 → app/clock causes a new snapshot render</div><p>Action names keep WidgetView Equatable and Sendable. Unknown IDs and non-interactive widgets are ignored intentionally.</p>`},
{id:'config',title:'Configuration and Environment',html:`<div class="tablewrap"><table><thead><tr><th>Scope</th><th>Stored values</th><th>Update behavior</th></tr></thead><tbody><tr><td>DashboardConfiguration</td><td>ID, theme, layout, refresh rate, environment values, services</td><td>Declarative builders copy; apply methods refresh attached widgets</td></tr><tr><td>WidgetConfiguration</td><td>ID, title, size, priority, visibility, refresh override, WidgetLayout, chrome</td><td>Captured with the attached widget</td></tr><tr><td>DashboardEnvironment</td><td>Resolved dashboard/widget IDs, theme, layout, rate, values, services</td><td>Created per attachment, update, tick, render, or action</td></tr></tbody></table></div><div class="card warning"><span class="tag important">Known debt</span><p>Typed key wrappers currently resolve through string names into <code>[String: Any]</code>. This is ergonomic but permits collisions and runtime-only type failures.</p></div>`},
{id:'decisions',title:'Architectural Constraints',html:`<ul><li>Theme changes adopt the theme's default layout unless the layout was explicitly pinned.</li><li>Hidden widgets do not receive ticks.</li><li>Widget-specific refresh rates override the dashboard rate.</li><li>Snapshot equality is used by the UI renderer to avoid redundant layout work.</li><li>SwiftCrossUI view node structure should remain stable during active gestures on GTK.</li><li>The HTTP server and SwiftCrossUI renderer are optional infrastructure conceptually, even though package products do not yet expose them.</li></ul>`}
],remember:['Dashboard owns lifecycle and snapshots; renderers own presentation.','WidgetContent and WidgetView are the portability seam.','Actions are data, not closures, so the rendering tree stays comparable and transferable.'],todo:['Trace one widget from service through renderer using the Application page.','Review the known boundary risks before changing public API.','Keep new dependencies in adapters unless the core truly requires them.']});

page({file:'framework.html',title:'DashboardKit Core Reference',sentence:'DashboardKit is the reusable engine: dashboard state, widget contracts, semantic views, themes, layouts, clocks, and snapshots.',sections:[
{id:'dashboard',title:'Dashboard Subsystem',html:`<div class="tablewrap"><table><thead><tr><th>File/type</th><th>Responsibility</th><th>Important API</th></tr></thead><tbody><tr><td>Dashboard.swift</td><td>Owns configuration, attached widgets, order, lifecycle, ticks, actions, environment, snapshots, placements</td><td><code>add</code>, <code>remove</code>, <code>tick</code>, <code>perform</code>, <code>applyTheme</code>, <code>attachedWidgetSnapshots</code></td></tr><tr><td>DashboardRunner.swift</td><td>Reference wrapper and clock driver</td><td><code>start</code>, <code>stop</code>, <code>perform</code></td></tr><tr><td>DashboardBuilder.swift</td><td>Result-builder DSL for declarative widget construction</td><td><code>Dashboard { ... }</code></td></tr><tr><td>AttachedWidget.swift</td><td>Internal erased widget plus public immutable snapshot</td><td><code>AttachedWidgetSnapshot</code></td></tr><tr><td>DashboardConfiguration.swift</td><td>Global theme, layout, refresh, values, and services</td><td>initializer and mutable properties</td></tr></tbody></table></div>`},
{id:'widgets',title:'Widget Contracts',html:`<div class="grid"><div class="card"><h3>Widget</h3><p>Base lifecycle contract with chainable modifiers for identity, title, size, priority, visibility, refresh, tile layout, and chrome.</p></div><div class="card"><h3>RenderableWidget</h3><p>Adds <code>render(environment:)</code> and returns semantic <code>WidgetContent</code>.</p></div><div class="card"><h3>ServiceBackedWidget</h3><p>Resolves a directly bound service, environment service, or fallback; owns model activation and teardown.</p></div><div class="card"><h3>InteractiveWidget</h3><p>Receives string action names emitted by renderer-interpreted WidgetView nodes.</p></div></div><p>A service-backed widget should normally implement configuration, service key, bound service, model storage, model/fallback factories, and render.</p>`},
{id:'semantic',title:'Semantic Rendering Model',html:`<p><code>WidgetView</code> is an indirect enum containing text roles, badges, spacers, stacks, centered groups, dividers, progress, play state, fitted text, tappable nodes, and minimum-size regions.</p><div class="card"><span class="tag must">Must Know</span><p>Widgets do not choose fonts or pixels. They choose semantic roles and structure; renderer plus theme determines actual presentation.</p></div><p><code>WidgetLayout</code> is an Equatable/Sendable named mapping from WidgetContent to WidgetView. Sixteen preset layouts live under <code>Widget/Layouts</code>.</p>`},
{id:'layout',title:'Dashboard Layout',html:`<div class="tablewrap"><table><thead><tr><th>Type</th><th>Meaning</th></tr></thead><tbody><tr><td>Layout</td><td>Sendable protocol mapping LayoutItems to placements and visibility</td></tr><tr><td>GridLayout</td><td>Column-count grid with span-aware placement generation</td></tr><tr><td>LayoutRegion</td><td>Named logical region</td></tr><tr><td>WidgetPlacement</td><td>Visibility plus optional grid slot</td></tr><tr><td>WidgetGridSlot</td><td>Row, column, row span, and column span</td></tr></tbody></table></div><p>Dashboard recomputes placement after additions, removals, and applied layout changes.</p>`},
{id:'theme',title:'Theme System',html:`<p><code>Theme</code> is Sendable and supplies sizes, colors, typography, spacing, shape, animation, and a default layout. ThemeMetrics converts design tokens for a viewport.</p><div class="grid"><div class="card"><h3>Built-in themes</h3><p>DefaultTheme, DarkDeskTheme, GreenBoardTheme, GradientClockTheme, and RuledGreenTheme.</p></div><div class="card"><h3>Token groups</h3><p>ThemeSizes, ThemeColors, ThemeTypography, ThemeSpacing, ThemeShape, and ThemeAnimation.</p></div></div>`},
{id:'time',title:'Clock and Refresh',html:`<p><code>DashboardClock</code> abstracts start/stop state. <code>ManualDashboardClock</code> makes time deterministic in tests; <code>TimerDashboardClock</code> drives production ticks.</p><p><code>RefreshRate</code> stores seconds. Dashboard-level rate drives the clock, while each widget may impose a slower delivery interval.</p>`},
{id:'risks',title:'Core Risks and Extension Rules',html:`<div class="card warning"><ul><li>DashboardRunner has mutable state without an explicit actor contract.</li><li>Configuration and environment use String/Any storage behind typed keys.</li><li>DashboardRenderer's default run method can own the main run loop.</li><li>Preset tile layouts may be product-specific API commitments if left in core.</li><li>WidgetContent has no renderer-neutral image/media descriptor yet.</li></ul></div><p>Prefer extending semantic types over adding backend-specific cases. Keep App services and business rules outside DashboardKit.</p>`}
],remember:['Dashboard is the state machine; DashboardRunner is the clock-driven reference wrapper.','ServiceBackedWidget removes repeated service/model lifecycle code.','WidgetLayout converts content into backend-neutral WidgetView structure.'],todo:['Read Dashboard.swift before changing lifecycle behavior.','Add ManualDashboardClock tests for timing changes.','Treat new public symbols as future compatibility commitments.']});

page({file:'renderers.html',title:'Renderer Layer Reference',sentence:'Renderers turn DashboardKit snapshots into terminal, browser, or native SwiftCrossUI output while preserving the core semantic model.',sections:[
{id:'contract',title:'Renderer Contract',html:`<p><code>DashboardRenderer</code> accepts an array of <code>AttachedWidgetSnapshot</code>. Its default <code>run()</code> enters the Foundation main loop.</p><div class="card warning"><span class="tag important">Embedding issue</span><p>A reusable adapter should separate snapshot consumption from standalone process lifecycle. Existing hosts must retain control of their own application entry point.</p></div>`},
{id:'dev',title:'Development Renderers',html:`<div class="grid"><div class="card"><h3>ConsoleRenderer</h3><p>Prints dashboard snapshots for the simplest dependency-free feedback path.</p><p>Use it for logic smoke tests and environments without a GUI.</p></div><div class="card"><h3>DevWebRenderer</h3><p>Converts snapshots to JSON-like tile payloads and serves a debug page through DashboardHTTPServer.</p><p>Use it for remote visibility and static-musl Pi development.</p></div></div>`},
{id:'ui',title:'SwiftCrossUI Renderer',html:`<p><code>SwiftCrossUIRenderer</code> bridges snapshots into a shared <code>DashboardModel</code> and launches <code>DashboardApp</code>. The model manages arrangements, selected board, edit state, hue mode, and UI options.</p><div class="flow">AttachedWidgetSnapshot[]
 → SwiftCrossUIRenderer.render
 → DashboardModel
 → DashboardRootView
 → BoardScreen
 → BoardColumn / BoardRow / BoardBand
 → TileView
 → semantic WidgetView node rendering</div>`},
{id:'boards',title:'Board Composition DSL',html:`<p><code>BoardColumn</code>, <code>BoardBand</code>, and <code>BoardRow</code> describe screen geometry. App-side extensions build named boards without putting DeskDashboard choices into the renderer target.</p><p><code>Arrangement</code> pairs a board composition with theme and display options. The root shell switches between arrangements and exposes edit controls.</p>`},
{id:'tile',title:'Tile Rendering Responsibilities',html:`<div class="tablewrap"><table><thead><tr><th>Component</th><th>Responsibility</th></tr></thead><tbody><tr><td>TileView</td><td>Tile chrome and semantic node dispatch</td></tr><tr><td>TileView+DisplayText</td><td>Text role mapping and main values</td></tr><tr><td>TileView+FittedText</td><td>Size-to-fit value rendering</td></tr><tr><td>TileView+Transport</td><td>Play/pause nodes and controls</td></tr><tr><td>EditScrim</td><td>Interactive edit overlay</td></tr><tr><td>ThemeToSCUIPalette</td><td>Maps DashboardKit theme tokens to native palette values</td></tr><tr><td>TextRoleToSCUIStyle</td><td>Maps semantic text roles to typography/color</td></tr></tbody></table></div>`},
{id:'gtk',title:'GTK-Specific Support',html:`<ul><li><code>PressReleaseGTK</code> bridges reliable press/release callbacks.</li><li><code>HoldGate</code> coordinates taps, holds, repeat timing, and gesture safety.</li><li><code>FrameTicker</code> drives animation cadence and optional diagnostics.</li><li><code>TextToGTKTracking</code> applies real letter-spacing through Gtk.Label inspection.</li><li><code>TileBorderGTK</code> and <code>TileCorners</code> address backend-specific chrome behavior.</li></ul><div class="card warning"><p>GTK can cancel an active gesture when the view node structure changes. Interactive layouts should keep node structure constant and vary strings/state instead.</p></div>`},
{id:'performance',title:'Performance Model',html:`<p>The renderer skips snapshots equal to the current value, avoiding expensive layout passes when clock-visible content has not changed. A local SwiftCrossUI patch reuses the widget Pango context during text measurement.</p><p>After those optimizations, view-tree node count is the main remaining cost. Restructure only with screenshot and touch-regression evidence.</p>`}
],remember:['Every renderer consumes the same semantic snapshots.','SwiftCrossUI currently mixes adapter behavior with app lifecycle ownership.','GTK gesture correctness depends on stable node structure.'],todo:['Use ConsoleRenderer when debugging framework state.','Use the Pi screenshot loop for native geometry changes.','Keep new backend work inside renderer targets, not DashboardKit core.']});

page({file:'application.html',title:'Application, Widgets, and Ingestion',sentence:'The App layer is the concrete DeskDashboard appliance: six feature families produce ten widget instances plus Pi-specific ingestion and operations.',sections:[
{id:'composition',title:'Composition Root',html:`<p><code>makeDeskDashboardSystem()</code> in <code>DeskDashboardComposition/Composition.swift</code> is the main dependency assembly point. It creates services, builds the dashboard, attaches widgets, creates the runner, and returns ingest-capable state.</p><div class="card"><span class="tag must">Must Know</span><p>When you need to understand what actually runs, start in Composition.swift rather than chasing each widget independently.</p></div>`},
{id:'widgets',title:'Widget Families',html:`<div class="tablewrap"><table><thead><tr><th>Family</th><th>Service/model</th><th>Main behavior</th></tr></thead><tbody><tr><td>Clock</td><td>ClockService → ClockWidgetModel</td><td>System time and display options</td></tr><tr><td>Alarm</td><td>AlarmService → AlarmWidgetModel</td><td>Local alarm state and presentation</td></tr><tr><td>Outdoor Temperature</td><td>OpenMeteo or simulated service → model</td><td>Async weather conditions</td></tr><tr><td>Indoor Temperature</td><td>Push or simulated service → model</td><td>Receives local network readings</td></tr><tr><td>Music</td><td>Push or simulated service → model</td><td>Now-playing metadata, progress, playback state</td></tr><tr><td>MTG</td><td>In-memory game service</td><td>Interactive life and turn counters</td></tr></tbody></table></div>`},
{id:'pattern',title:'Service-Backed Widget Pattern',html:`<div class="flow">Service protocol + ServiceKey
 → concrete real/push/simulated service
 → WidgetModel activates and reads service
 → Widget renders WidgetContent from model state
 → selected WidgetLayout produces WidgetView</div><p>Fallback services keep widgets usable in development. Direct widget binding overrides the dashboard environment; environment registration overrides the fallback.</p>`},
{id:'ingest',title:'Push Ingestion',html:`<p><code>DeskDashboardIngest/PushIngest.swift</code> defines app-specific HTTP routes for pushed indoor-temperature and now-playing data. <code>registerPushIngest</code> wires those routes to shared stores.</p><p>Both the dev executable and native UI can run the same ingest server, normally on port 8642.</p><div class="card warning"><p>The HTTP layer has no TLS or authentication and is intended for trusted local-network use. Do not expose it directly to the public internet.</p></div>`},
{id:'executables',title:'Executable Entry Points',html:`<div class="grid"><div class="card"><h3>deskdashboard-dev</h3><p>Dependency-light development executable using console or web rendering. This is the static-musl cross-compile path.</p></div><div class="card"><h3>deskdashboard-ui</h3><p>Native SwiftCrossUI/GTK appliance. It parses kiosk, scale, window, slide, and frame options before launching the renderer.</p></div></div>`},
{id:'boards',title:'Arrangements and Boards',html:`<p><code>Arrangements.swift</code> defines the selectable presentation catalog. Board extensions such as FocusBoard, MTGBoard, RuledBoard, EqualWidthsBoard, and WideClockBoard define concrete geometry with renderer-owned board primitives.</p><p>The background image path is currently machine-specific. Treat resource ownership as app configuration or a declared SwiftPM resource rather than a hard-coded filesystem assumption.</p>`},
{id:'extend',title:'How to Add a Widget',html:`<ol><li>Define a service protocol and typed ServiceKey.</li><li>Add a real, push, local, or simulated service implementation.</li><li>Create a WidgetModel for activation, updates, ticks, and deactivation.</li><li>Create a ServiceBackedWidget and render WidgetContent.</li><li>Choose or add a WidgetLayout using semantic WidgetView nodes.</li><li>Register the service and widget in Composition.swift.</li><li>Add focused model/widget tests and verify the live board.</li></ol>`}
],remember:['Composition.swift is the truth for the running appliance.','Each major data widget follows Service → Model → Widget → semantic content.','Ingest is app-specific local-network infrastructure, not a core DashboardKit requirement.'],todo:['Trace MusicWidget for the richest service-backed example.','Use MTG widgets for interactive action examples.','Update Composition and tests together when adding an app capability.']});

page({file:'build-test-ops.html',title:'Build, Test, and Operations Guide',sentence:'The project has separate development, native UI, and kiosk paths because SwiftCrossUI/GTK cannot use the static-musl cross-compile route.',sections:[
{id:'package',title:'Swift Package Products and Targets',html:`<div class="tablewrap"><table><thead><tr><th>Product</th><th>Target path</th><th>Purpose</th></tr></thead><tbody><tr><td>DashboardKit library</td><td>Sources/Framework/DashboardKit</td><td>Reusable engine</td></tr><tr><td>deskdashboard-dev</td><td>Sources/App/DeskDashboardDevApp</td><td>Console/web development front end</td></tr><tr><td>deskdashboard-ui</td><td>Sources/App/DeskDashboardApp</td><td>Native SwiftCrossUI application</td></tr></tbody></table></div><p>Other framework and renderer targets are internal dependencies today, not selectable library products.</p>`},
{id:'local',title:'Local Development',html:`<pre><code>swift build
swift test
swift run deskdashboard-dev
swift run deskdashboard-ui</code></pre><p>The package uses Swift tools 6.3 and Swift language mode 6. The package manifest currently declares macOS 13.</p><div class="card warning"><p>If SwiftPM reports another process using <code>.build</code>, wait for or stop the known build process. Do not delete build state blindly while another process is active.</p></div>`},
{id:'tests',title:'Test Coverage Map',html:`<div class="tablewrap"><table><thead><tr><th>Test group</th><th>Coverage</th></tr></thead><tbody><tr><td>DeskDashboardTests</td><td>Core dashboard construction and behavior</td></tr><tr><td>DashboardRunnerObserverTests</td><td>Runner tick/observer behavior</td></tr><tr><td>InteractiveWidgetTests</td><td>Named action routing</td></tr><tr><td>WidgetLayoutTests</td><td>Semantic layout construction</td></tr><tr><td>ThemeMetricsTests</td><td>Viewport-derived theme metrics</td></tr><tr><td>Widget family tests</td><td>Clock, alarm, indoor/outdoor temperature, music</td></tr></tbody></table></div><p>Current gaps include external-consumer compilation, renderer/HTTP tests, platform matrix CI, resource lookup, and API compatibility checks.</p>`},
{id:'pi',title:'Raspberry Pi Build Split',html:`<div class="grid"><div class="card"><h3>Static development binary</h3><p><code>scripts/build-pi.sh</code> cross-compiles the dependency-light development executable with the static-musl SDK.</p></div><div class="card"><h3>Native GTK UI</h3><p><code>scripts/build-ui-pi.sh</code> builds on the Pi with glibc and GTK 4. It limits jobs based on available RAM.</p></div></div><p>The UI build reapplies <code>patches/swift-cross-ui-pango-context.patch</code> after dependency resolution to retain the Pango measurement optimization.</p>`},
{id:'deploy',title:'Kiosk Deployment',html:`<p><code>scripts/install-kiosk-pi.sh</code> installs the fullscreen session and systemd restart behavior. The Pi currently uses cage/sway configuration to launch the release binary.</p><ul><li>Build with <code>CONFIG=release JOBS=1</code> for the shipping kiosk path.</li><li>Confirm the configured binary path before restarting the service.</li><li>Use <code>grim</code> over SSH for screenshots.</li><li>Use sway IPC for remote taps, but do not treat synthetic tap throughput as equivalent to real touch.</li></ul>`},
{id:'producer',title:'External Producers',html:`<p><code>producers/now-playing-push.py</code> sends macOS now-playing state to the Pi ingest endpoint. <code>install-nowplaying-agent.sh</code> installs its launch agent.</p><p>Producer restart behavior after reboot/failure is an operational verification item, not framework behavior.</p>`},
{id:'safety',title:'Operational Safety Notes',html:`<div class="card warning"><ul><li>The ingest server is trusted-LAN only unless hardened behind a secure boundary.</li><li>Native Pi release builds are slow and memory-sensitive; use one build job.</li><li>Tar-based deploys do not delete renamed files and can leave duplicate Swift declarations.</li><li>macOS tar can create AppleDouble files unless <code>COPYFILE_DISABLE=1</code> is set.</li><li>The SwiftCrossUI dependency patch may stop applying after an upstream version change; inspect warnings.</li></ul></div>`}
],remember:['There are two Pi build paths because GTK cannot use the static-musl route.','Tests cover core/widgets well but do not yet prove external package consumption.','The kiosk and ingest environment have real operational constraints that are documented separately from framework behavior.'],todo:['Run swift test when no other SwiftPM process owns .build.','Use the native UI script and screenshot loop for GTK changes.','Add downstream consumer and CI checks before treating the package as 1.0-ready.']});

page({file:'file-map.html',title:'Codebase File Map',sentence:'Use this page to jump from a behavior to the directory and file family that owns it.',sections:[
{id:'framework',title:'Framework Files',html:`<div class="tablewrap"><table><thead><tr><th>Directory</th><th>Files</th><th>Owns</th></tr></thead><tbody><tr><td>Dashboard</td><td>Dashboard, Configuration, Runner, Builder, ID, AttachedWidget</td><td>State, lifecycle, snapshots, DSL</td></tr><tr><td>Environment</td><td>EnvironmentKey, DashboardEnvironment</td><td>Typed lookup wrappers and per-widget context</td></tr><tr><td>Layout</td><td>Layout, LayoutRegion, WidgetPlacement, GridLayout</td><td>Dashboard-level placement</td></tr><tr><td>Theme</td><td>Theme, ThemeMetrics, five theme implementations</td><td>Backend-neutral design tokens</td></tr><tr><td>Widget</td><td>Widget, configuration, content, view, layouts, interaction, service scaffold</td><td>Widget authoring and semantic rendering</td></tr><tr><td>WidgetModel</td><td>WidgetModel</td><td>Service lifecycle bridge</td></tr><tr><td>Support</td><td>Clock, tick, refresh, UUID factory</td><td>Time and identity helpers</td></tr><tr><td>Rendering</td><td>DashboardRenderer</td><td>Snapshot consumer contract</td></tr><tr><td>DashboardHTTPServer</td><td>HTTPServer</td><td>Minimal socket HTTP server</td></tr></tbody></table></div>`},
{id:'renderer',title:'Renderer Files',html:`<div class="tablewrap"><table><thead><tr><th>Directory</th><th>Owns</th></tr></thead><tbody><tr><td>DashboardDevRenderers</td><td>Console and debug web output</td></tr><tr><td>DashboardUI/Renderer</td><td>Adapter, observable model, arrangements, launch options</td></tr><tr><td>DashboardUI/Shell</td><td>Root view, board switcher, edit controls</td></tr><tr><td>DashboardUI/Screens + Boards</td><td>Board geometry and screen assembly</td></tr><tr><td>DashboardUI/Tiles</td><td>Tile chrome and semantic node rendering</td></tr><tr><td>DashboardUI/Theme</td><td>Theme and text-role conversion</td></tr><tr><td>DashboardUI/Support</td><td>GTK gestures, tracking, borders, frame timing</td></tr></tbody></table></div>`},
{id:'app',title:'Application Files',html:`<div class="tablewrap"><table><thead><tr><th>Directory</th><th>Owns</th></tr></thead><tbody><tr><td>DeskDashboardComposition</td><td>Dependency assembly and shared ingest registration</td></tr><tr><td>DeskDashboardWidgets</td><td>Clock, alarm, temperature, music, and MTG domains</td></tr><tr><td>DeskDashboardIngest</td><td>App-specific push routes and payload handling</td></tr><tr><td>DeskDashboardDevApp</td><td>Development executable</td></tr><tr><td>DeskDashboardApp/main.swift</td><td>Native executable configuration and launch</td></tr><tr><td>DeskDashboardApp/Dashboard</td><td>Concrete board arrangements</td></tr></tbody></table></div>`},
{id:'tests',title:'Tests and Documentation',html:`<p><span class="file">Tests/DeskDashboardTests</span> contains framework behavior, theme/layout, observer, interaction, and widget family tests. Shared service doubles live in <code>ServiceStubs.swift</code>.</p><p><span class="file">docs/</span> contains Pi setup/deploy notes, UI build findings, widget/theme extension guides, progress records, and agent handoffs. These are operational history and contributor context rather than a consumer-facing package manual.</p>`},
{id:'scripts',title:'Scripts, Patch, and Assets',html:`<ul><li><code>scripts/start-dashboard.sh</code> starts the dashboard.</li><li><code>scripts/build-pi.sh</code> cross-compiles the dev binary.</li><li><code>scripts/build-ui-pi.sh</code> performs native low-memory UI builds.</li><li><code>scripts/install-kiosk-pi.sh</code> installs kiosk/service configuration.</li><li><code>producers/</code> contains the macOS now-playing push agent.</li><li><code>patches/swift-cross-ui-pango-context.patch</code> carries the GTK text measurement optimization.</li><li><code>Assets/tea-mist-wide.png</code> is the current dashboard background asset.</li></ul>`},
{id:'change',title:'Where to Make a Change',html:`<div class="tablewrap"><table><thead><tr><th>If you want to…</th><th>Start here</th></tr></thead><tbody><tr><td>Change widget lifecycle</td><td>Dashboard.swift and Widget.swift</td></tr><tr><td>Add a data widget</td><td>DeskDashboardWidgets plus Composition.swift</td></tr><tr><td>Add a semantic tile layout</td><td>DashboardKit/Widget/Layouts</td></tr><tr><td>Change board geometry</td><td>DashboardUI/Boards and App/Dashboard</td></tr><tr><td>Change native tile drawing</td><td>DashboardUI/Tiles</td></tr><tr><td>Change theme tokens</td><td>DashboardKit/Theme</td></tr><tr><td>Change GTK gesture behavior</td><td>DashboardUI/Support</td></tr><tr><td>Change push endpoints</td><td>DeskDashboardIngest and Composition</td></tr><tr><td>Change Pi launch behavior</td><td>scripts and docs/pi-*.md</td></tr></tbody></table></div>`}
],remember:['Start at the layer that owns the behavior, not the file with the visible symptom.','Composition.swift connects the app-specific graph; Package.swift connects the module graph.','GTK-specific behavior is intentionally quarantined under DashboardUI/Support.'],todo:['Use the hub to choose the right deeper guide.','Read the owning file and its nearest tests together.','Update the relevant HTMLer page when a major boundary changes.']});

console.log(`Generated ${links.length} standalone HTML files in ${output}`);
